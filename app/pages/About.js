import React from 'react';

const About = () => (
  <div>This is a to-do list that allows you to add tasks, check them off when they are completed, and delete them once the day is over.</div>
);

export default About;
